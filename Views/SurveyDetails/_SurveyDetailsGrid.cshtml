@using SurveyApp.Models
@model SurveyDetailsLocationModel

@{
    // Use a unique ID based on the ItemTypeID for the container
    var collapseId = $"collapse_{Model.ItemTypeID}";
}

@if (!string.IsNullOrWhiteSpace(Model.TypeDesc))
{
    <div class="mb-3 mt-3 p-3 bg-light rounded border-start border-4 border-info">
        <p class="mb-0 text-dark">
            <i class="bi bi-info-circle text-info me-2"></i><strong>Description:</strong> @Model.TypeDesc
        </p>
    </div>
}

@if (Model.ItemLists != null && Model.ItemLists.Any())
{
    <div class="row g-3">
        @foreach (var item in Model.ItemLists)
        {
            <div class="col-12 col-md-6 col-lg-4">
                <div class="card h-100 shadow-sm border-0 hover-lift">
                    <div class="card-header card-h bg-light border-0 d-flex justify-content-between align-items-center">
                        <h6 class="mb-0 fw-semibold text-dark">@item.ItemName</h6>
                        <span class="badge bg-purple">ID: @item.ItemID</span>
                    </div>
                    <div class="card-body">
                        <div class="row mb-3">
                            <div class="col-6">
                                <small class="text-muted d-block mb-1">
                                    <i class="bi bi-box me-1"></i><strong>Existing:</strong>
                                </small>
                                <span class="badge bg-secondary fs-6">@item.ItemQtyExist</span>
                            </div>
                            <div class="col-6">
                                <small class="text-muted d-block mb-1">
                                    <i class="bi bi-cart-plus me-1"></i><strong>Required:</strong>
                                </small>
                                <span class="badge bg-success fs-6">@item.ItemQtyReq</span>
                            </div>
                        </div>

                        @if (!string.IsNullOrWhiteSpace(item.Remarks))
                        {
                            <div class="mb-2">
                                <small class="text-muted d-block mb-1">
                                    <i class="bi bi-chat-left-text me-1"></i><strong>Remarks:</strong>
                                </small>
                                <p class="text-dark small mb-0">@item.Remarks</p>
                            </div>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
    <div class="text-center mt-4">
        <button type="button" class="btn btn-primary load-item-selection"
                data-surveyid="@Model.SurveyID"
                data-locid="@Model.LocID"
                data-itemtypeid="@Model.ItemTypeID"
                data-target="#item-selection-@collapseId">
            <i class="bi bi-plus-circle me-2"></i> View / Edit
        </button>
    </div>
    <div id="item-selection-@collapseId" class="item-selection-container mt-3">
        <!-- Partial view will be loaded here -->
    </div>
}
else
{
    <div class="text-center py-5">
        <i class="bi bi-inbox fs-1 text-muted d-block mb-3"></i>
        <p class="text-muted fs-5 mb-3">No items added yet for this device type.</p>
        <button type="button" class="btn btn-primary load-item-selection"
                data-surveyid="@Model.SurveyID"
                data-locid="@Model.LocID"
                data-itemtypeid="@Model.ItemTypeID"
                data-target="#item-selection-@collapseId">
            <i class="bi bi-play-circle me-2"></i> Start Survey
        </button>
    </div>
    <div id="item-selection-@collapseId" class="item-selection-container mt-3">
        <!-- Partial view will be loaded here -->
    </div>
}
